# Etapa de compilación
FROM mcr.microsoft.com/dotnet/sdk:9.0 AS build-env
WORKDIR /app

# Copiar el archivo del proyecto y restaurar dependencias
COPY *.csproj ./
RUN dotnet restore

# Instalar el paquete iTextSharp (para generar PDFs)
RUN dotnet add package iTextSharp --version 5.5.13.3

# Copiar todo el contenido y publicar la aplicación
COPY . ./
RUN dotnet publish -c Release -o out

# Etapa de ejecución
FROM mcr.microsoft.com/dotnet/aspnet:9.0
WORKDIR /app
COPY --from=build-env /app/out .

# Nombre del archivo principal a ejecutar
ENV APP_NET_CORE proyectoIngSoft.dll

# Ejecutar la aplicación
CMD ASPNETCORE_URLS=http://*:$PORT dotnet $APP_NET_CORE
